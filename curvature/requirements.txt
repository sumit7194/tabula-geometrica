# curvature/ — pinned to the versions this project was built and gated against.
# Setup:  python3.12 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
torch==2.12.0
numpy==2.4.6
scikit-learn==1.9.0
matplotlib==3.10.9
scipy==1.17.1
